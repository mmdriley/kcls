# Accessing KCLS via BiblioCommons REST API

I've kind of lost track of where the data in `endpoints.txt` came from. Archive? Web source? Since-forgotten GitHub repository?

The code in `login.py` is enough to get a session ID and show it can be used for one superficial-level request, but results so far have been inconclusive on whether the `session_id` from there is enough to retrieve `WEB` (vs. `REST`) endpoints.
